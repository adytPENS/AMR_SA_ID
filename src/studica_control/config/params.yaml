control_server:
  ros__parameters:
    use_sim_time: false
    imu:
      enabled: true
      name: "imu"
      topic: "imu"
      frame_id: "imu_link"
    # Configure your robot here before launching.
    # See params_template.yaml for the full parameter reference.
